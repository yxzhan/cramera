.direnv/
tmp/
*.mypy_cache/
build/
data/
test/tmp_data/
bin/
lib/
msg_gen/
srv_gen/
msg/*Action.msg
msg/*ActionFeedback.msg
msg/*ActionGoal.msg
msg/*ActionResult.msg
msg/*Feedback.msg
msg/*Goal.msg
msg/*Result.msg
msg/_*.py
*muh*
*.txt
# The hook tests' scratch clone needs a real requirements.txt at the path
# check-setup.sh looks for, so this one fixture is checked in.
!.claude/hooks/tests/fixtures/set-up-clone/**/*.txt
*.png
!.claude/skills/plan-dashboard/example/screenshots/*.png
*.lprof
tmp_data/
tmp/
.idea/
*.egg-info/

# Generated by dynamic reconfigure
*.cfgc
/cfg/cpp/
/cfg/*.py

# Ignore generated doc
*.dox
*.wikidoc

# eclipse stuff
.project
.cproject

# qcreator stuff
CMakeLists.txt.user

# python stuff
*.hypothesis/
*.pytest_cache/
srv/_*.py
*.pcd
*.pyc
*.pyo
qtcreator-*
*.user
*.prof

/planning/cfg
/planning/docs
/planning/src

*~

# Emacs
.#*

# VS Code stuff
.vscode/

# Catkin custom files
CATKIN_IGNORE

# Package Managers
poetry.lock
uv.lock

# Created during tests
file::memory:

dist

# probabilistic model dependent
mlruns
mlartifacts
.drawio

# Documentation build artifacts
**/_build/
**/.jupyter_cache/
**/test_tmp/
semantic_digital_twin/resources/output

# test generated files
*.pkl
test/krrood_test/test_ripple_down_rules/*.dot
test/krrood_test/test_ripple_down_rules/*.svg
test/krrood_test/test_ripple_down_rules/test_results/
test/**/*.pdf
test/krrood_test/test_ripple_down_rules/test_generated_rdrs

!semantic_digital_twin/resources/ply/chair_texture.png


### CPP IGNORES
# Prerequisites
*.d

# Compiled Object files
*.slo
*.lo
*.o
*.obj

# Precompiled Headers
*.gch
*.pch

# Compiled Dynamic libraries
*.so
*.dylib
*.dll

# Fortran module files
*.mod
*.smod

# Compiled Static libraries
*.lai
*.la
*.a
*.lib

# Executables
*.exe
*.out
*.app

doc/doc_out

# Bazel
/bazel-*

# Directories for the Bazel IntelliJ plugin containing the generated
# IntelliJ project files and plugin configuration. Seperate directories are
# for the IntelliJ, Android Studio and CLion versions of the plugin.
/.ijwb/
/.aswb/
/.clwb/

# Generated visualization artifacts: graph / class-diagram renders and RDR rule
# trees written into the working tree by visualize(), render_tree() and
# render_evaluated_rule_tree() during tests and doc builds. None are committed.
pdf_graph.pdf
query_graph.pdf
class_diagram.pdf
*_explanation.pdf
*_read_tree.dot
*_read_tree.svg
*_evaluated_tree.dot
*_evaluated_tree.svg
*_rdr.dot
*_rdr.svg

# Personal, per-user Claude Code memory (populated by .claude/hooks/session-start.sh
# from the claude/personal-notes branch). Never committed on any branch.
CLAUDE.local.md

# Personal, per-user Claude Code settings (populated by the same hook from the same
# branch), plus the stamp recording what it last synced there. Never committed on any
# branch - shared project settings live in the committed .claude/settings.json instead.
.claude/settings.local.json
.claude/.personal-settings-sync-hash

# Stamp recording the personal-notes commit this clone last read plan state
# at (written by session-start.sh, read/advanced by plan-updates-since.sh).
# Never committed - it's a per-clone baseline, not shared state.
.claude/.plan-state-sync-sha

# The stacked-PR board snapshot, re-exported from GitHub by
# `.claude/stack/maintenance.py board --write` at the start of every maintenance pass.
# Scratch state, never committed: a stale copy read as current is a pass acting on a
# stack that has since moved.
.claude/stack/board.json

# ORMatic generates every package's ormatic_interface.py from its dataclasses, so the
# repository builds them (scripts/regenerate_all_orm.py) instead of tracking them.
# Tracking a generated file made every branch switch fail while a checkout held its own
# copy, which is what git refuses to overwrite.
**/ormatic_interface.py
